import type { Rule } from '@unocss/core'
import type { Theme } from './theme'
import { toArray } from '@unocss/core'
import {
  alignments,
  appearance,
  accessibility,
  appearances,
  aspectRatio,
  backgroundBlendModes,
  bgColors,
  borders,
  boxShadowsBase,
  boxShadows,
  boxSizing,
  breaks,
  colorScheme,
  contains,
  contents,
  contentVisibility,
  cursors,
  displays,
  fieldSizing,
  flex,
  flexGridJustifiesAlignments,
  floats,
  fonts,
  fontSmoothings,
  fontStyles,
  hyphens,
  gaps,
  grids,
  insets,
  isolations,
  justifies,
  margins,
  mixBlendModes,
  opacity,
  objectFits,
  objectPositions,
  orders,
  outline,
  overflows,
  paddings,
  placements,
  placeholderColors,
  pointerEvents,
  positions,
  resizes,
  rings,
  ringBase,
  sizes,
  svgUtilities,
  tabSizes,
  textAligns,
  textDecorations,
  textIndents,
  textOverflows,
  textShadows,
  textStrokes,
  textTransforms,
  textWraps,
  transitions,
  transforms,
  transformBase,
  userSelects,
  verticalAligns,
  whitespaces,
  willChange,
  zIndexes,
} from './_rules'
import {
  accents,
  animations,
  backgroundStyles,
  carets,
  columns,
  container,
  divides,
  filters,
  fontVariantNumeric,
  lineClamps,
  listStyle,
  overscrolls,
  spaces,
  scrollBehaviors,
  scrolls,
  tables,
  touchActions,
} from './_rules-wind3'

export const rules: Rule<Theme>[] = [
  ...toArray(contains),
  ...toArray(accessibility),
  ...toArray(pointerEvents),
  ...toArray(appearances),
  ...toArray(isolations),
  ...toArray(positions),
  ...toArray(insets),
  ...toArray(zIndexes),
  ...toArray(orders),
  ...toArray(grids),
  ...toArray(floats),
  ...toArray(margins),
  ...toArray(boxSizing),
  ...toArray(displays),
  ...toArray(objectFits),
  ...toArray(objectPositions),
  ...toArray(aspectRatio),
  ...toArray(sizes),
  ...toArray(flex),
  ...toArray(transforms),
  ...toArray(cursors),
  ...toArray(userSelects),
  ...toArray(resizes),
  ...toArray(appearance),
  ...toArray(placements),
  ...toArray(alignments),
  ...toArray(justifies),
  ...toArray(gaps),
  ...toArray(flexGridJustifiesAlignments),
  ...toArray(overflows),
  ...toArray(textOverflows),
  ...toArray(whitespaces),
  ...toArray(breaks),
  ...toArray(hyphens),
  ...toArray(borders),
  ...toArray(bgColors),
  ...toArray(backgroundBlendModes),
  ...toArray(mixBlendModes),
  ...toArray(placeholderColors),
  ...toArray(colorScheme),
  ...toArray(svgUtilities),
  ...toArray(paddings),
  ...toArray(textAligns),
  ...toArray(textIndents),
  ...toArray(textWraps),
  ...toArray(verticalAligns),
  ...toArray(fonts),
  ...toArray(textTransforms),
  ...toArray(fontStyles),
  ...toArray(textDecorations),
  ...toArray(fontSmoothings),
  ...toArray(tabSizes),
  ...toArray(textStrokes),
  ...toArray(textShadows),
  ...toArray(opacity),
  ...toArray(boxShadows),
  ...toArray(outline),
  ...toArray(rings),
  ...toArray(transitions),
  ...toArray(willChange),
  ...toArray(contentVisibility),
  ...toArray(contents),
  ...toArray(fieldSizing),
  ...container,
  ...animations,
  ...scrolls,
  ...spaces,
  ...backgroundStyles,
  ...divides,
  ...filters,
  ...fontVariantNumeric,
  ...columns,
  ...lineClamps,
  ...tables,
  ...touchActions,
  ...listStyle,
  ...accents,
  ...carets,
  ...overscrolls,
  ...scrollBehaviors,
  [/^font-\[(\d+)\]$/, ([, value]) => ({ 'font-weight': value })],
]

export {
  boxShadowsBase,
  ringBase,
  transformBase,
}
